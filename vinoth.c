#include<stdio.h>
#include<conio.h>
int main()
{
int i,sum,n,k,a[5];
printf("Enter the two number :\n");
scanf("%d%d",&n,&k);
printf("enter the values :");
for(i=o;i<n;i++)
scanf("%d",&a[i]);
}
sum=k+a[0];
printf("the sum is %d",sum);
return 0;
}
